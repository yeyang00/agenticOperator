// AUTO-GENERATED by scripts/gen-action-snapshot.ts at 2026-05-08T08:28:48.756Z
// source: action=packageSupplement id=14-2 domain=RAAS-v1 template=v3
// DO NOT EDIT — regenerate via: npm run gen:ontology -- --action-name packageSupplement

import type { ActionObject } from "./action-object.types";

export const packageSupplementActionObject: ActionObject = {
  id: "14-2",
  name: "packageSupplement",
  description: "推荐包信息补全 - 招聘专员补全推荐包缺失的必填信息",
  submissionCriteria: "",
  category: "人工操作",
  actor: [
    "Human"
  ],
  trigger: [
    "PACKAGE_MISSING_INFO"
  ],
  targetObjects: [
    "Recommendation_Material",
    "Candidate",
    "Resume",
    "Compliance_Document"
  ],
  inputs: [
    {
      "description": "待补全的推荐包唯一编号。",
      "name": "recommendation_material_id",
      "required": true,
      "sourceObject": "Recommendation_Material.recommendation_material_id",
      "type": "String"
    },
    {
      "description": "候选人唯一编号。",
      "name": "candidate_id",
      "required": true,
      "sourceObject": "Candidate.candidate_id",
      "type": "String"
    }
  ],
  outputs: [
    {
      "description": "补全结果状态。值为\"已补全\"(所有缺失项已补齐)。",
      "name": "supplement_status",
      "type": "String"
    }
  ],
  actionSteps: [
    {
      "description": "招聘专员根据系统提示的缺失项清单，联系候选人补全推荐包中缺失的必填信息（如作品集、空窗期原因、合规凭证等），将补全后的材料上传至系统。完成后触发RESUME_OPTIMIZED事件重新执行推荐包生成和完整性校验。",
      "inputs": [
        {
          "description": "推荐包编号。",
          "name": "recommendation_material_id",
          "sourceObject": "Recommendation_Material.recommendation_material_id",
          "type": "String"
        },
        {
          "description": "候选人编号。",
          "name": "candidate_id",
          "sourceObject": "Candidate.candidate_id",
          "type": "String"
        }
      ],
      "name": "补全推荐包信息",
      "objectType": "action",
      "order": 1,
      "outputs": [
        {
          "description": "补全结果状态。",
          "name": "supplement_status",
          "type": "String"
        }
      ],
      "rules": []
    }
  ],
  sideEffects: {
    "dataChanges": [
      {
        "action": "MODIFY",
        "description": "补全空窗期原因等候选人信息。",
        "objectType": "Candidate",
        "propertyImpacted": [
          "gap_reason"
        ]
      },
      {
        "action": "CREATE",
        "description": "上传合规证明材料（如回流凭证、白名单确认等）。",
        "objectType": "Compliance_Document",
        "propertyImpacted": [
          "compliance_document_id",
          "application_id",
          "candidate_id",
          "document_type",
          "file_url",
          "verification_status"
        ]
      }
    ],
    "notifications": []
  },
  triggeredEvents: [
    "RESUME_OPTIMIZED"
  ],

  prompt: `## Action specification: packageSupplement

The block below is the formal specification of the action \`packageSupplement\`. It is part of a larger user message; the runtime supplies inputs and additional context separately. Treat this block as a binding contract: do not deviate from the declared steps, do not write outside the declared targets, and do not return outputs outside the declared schema. If preconditions are unmet or required inputs are missing, follow Error policy.

- Category: 人工操作
- Actor:    Human
- Trigger:  PACKAGE_MISSING_INFO

## Purpose
推荐包信息补全 - 招聘专员补全推荐包缺失的必填信息

## Inputs
The runtime provides these as a JSON object on the inbound payload; field names match exactly. Validate before proceeding; if a required field is missing, follow Error policy.

- recommendation_material_id (String, required) ← Recommendation_Material.recommendation_material_id: 待补全的推荐包唯一编号。
- candidate_id (String, required) ← Candidate.candidate_id: 候选人唯一编号。

## Steps
1. 补全推荐包信息 [action]
   description:  招聘专员根据系统提示的缺失项清单，联系候选人补全推荐包中缺失的必填信息（如作品集、空窗期原因、合规凭证等），将补全后的材料上传至系统。完成后触发RESUME_OPTIMIZED事件重新执行推荐包生成和完整性校验。
   inputs:
   - recommendation_material_id (String) ← Recommendation_Material.recommendation_material_id — 推荐包编号。
   - candidate_id (String) ← Candidate.candidate_id — 候选人编号。
   outputs:
   - supplement_status (String) — 补全结果状态。

## Output
Return a JSON object matching this schema. The fields below are required; do not include extra keys.

- supplement_status (String): 补全结果状态。值为"已补全"(所有缺失项已补齐)。

\`\`\`json
{
  "supplement_status": "<String>"
}
\`\`\`

## Side-effect boundary

### writes
- Candidate / MODIFY
   fields: gap_reason
   note:   补全空窗期原因等候选人信息。
- Compliance_Document / CREATE
   fields: compliance_document_id, application_id, candidate_id, document_type, file_url, verification_status
   note:   上传合规证明材料（如回流凭证、白名单确认等）。

### reads
Recommendation_Material, Resume

### emits
- RESUME_OPTIMIZED

Anything outside the above is out of scope; refuse and surface via Error policy.

## Error policy
- Missing or invalid required input → return {"status":"failed","reason":"<which field>","retry":false}; do not partially execute.
- Upstream resource unreachable or auth failure → return {"status":"failed","reason":"<...>","retry":true}.

## Completion criteria
The action is complete when ALL of:

1. Each step has run to its declared termination, or its failure has been surfaced per Error policy.
2. The Output JSON contains all required keys (supplement_status) and conforms to the schema in Output.
3. Any required outbound event has been emitted (success events on success; alert events on failure).

## Before returning
Verify all of the following before responding:

- Output JSON conforms to the Output schema (no extra or missing keys).
- No write occurred outside Side-effect boundary.
- Every required notification or outbound event has been emitted.`,

  sections: {
    "actionSpec": "## Action specification: packageSupplement\n\nThe block below is the formal specification of the action `packageSupplement`. It is part of a larger user message; the runtime supplies inputs and additional context separately. Treat this block as a binding contract: do not deviate from the declared steps, do not write outside the declared targets, and do not return outputs outside the declared schema. If preconditions are unmet or required inputs are missing, follow Error policy.\n\n- Category: 人工操作\n- Actor:    Human\n- Trigger:  PACKAGE_MISSING_INFO",
    "beforeReturning": "## Before returning\nVerify all of the following before responding:\n\n- Output JSON conforms to the Output schema (no extra or missing keys).\n- No write occurred outside Side-effect boundary.\n- Every required notification or outbound event has been emitted.",
    "completionCriteria": "## Completion criteria\nThe action is complete when ALL of:\n\n1. Each step has run to its declared termination, or its failure has been surfaced per Error policy.\n2. The Output JSON contains all required keys (supplement_status) and conforms to the schema in Output.\n3. Any required outbound event has been emitted (success events on success; alert events on failure).",
    "errorPolicy": "## Error policy\n- Missing or invalid required input → return {\"status\":\"failed\",\"reason\":\"<which field>\",\"retry\":false}; do not partially execute.\n- Upstream resource unreachable or auth failure → return {\"status\":\"failed\",\"reason\":\"<...>\",\"retry\":true}.",
    "inputsSpec": "## Inputs\nThe runtime provides these as a JSON object on the inbound payload; field names match exactly. Validate before proceeding; if a required field is missing, follow Error policy.\n\n- recommendation_material_id (String, required) ← Recommendation_Material.recommendation_material_id: 待补全的推荐包唯一编号。\n- candidate_id (String, required) ← Candidate.candidate_id: 候选人唯一编号。",
    "output": "## Output\nReturn a JSON object matching this schema. The fields below are required; do not include extra keys.\n\n- supplement_status (String): 补全结果状态。值为\"已补全\"(所有缺失项已补齐)。\n\n```json\n{\n  \"supplement_status\": \"<String>\"\n}\n```",
    "preconditions": null,
    "purpose": "## Purpose\n推荐包信息补全 - 招聘专员补全推荐包缺失的必填信息",
    "ruleIndex": null,
    "sideEffectBoundary": "## Side-effect boundary\n\n### writes\n- Candidate / MODIFY\n   fields: gap_reason\n   note:   补全空窗期原因等候选人信息。\n- Compliance_Document / CREATE\n   fields: compliance_document_id, application_id, candidate_id, document_type, file_url, verification_status\n   note:   上传合规证明材料（如回流凭证、白名单确认等）。\n\n### reads\nRecommendation_Material, Resume\n\n### emits\n- RESUME_OPTIMIZED\n\nAnything outside the above is out of scope; refuse and surface via Error policy.",
    "steps": "## Steps\n1. 补全推荐包信息 [action]\n   description:  招聘专员根据系统提示的缺失项清单，联系候选人补全推荐包中缺失的必填信息（如作品集、空窗期原因、合规凭证等），将补全后的材料上传至系统。完成后触发RESUME_OPTIMIZED事件重新执行推荐包生成和完整性校验。\n   inputs:\n   - recommendation_material_id (String) ← Recommendation_Material.recommendation_material_id — 推荐包编号。\n   - candidate_id (String) ← Candidate.candidate_id — 候选人编号。\n   outputs:\n   - supplement_status (String) — 补全结果状态。"
  },

  meta: {
    "actionId": "14-2",
    "actionName": "packageSupplement",
    "compiledAt": "2026-05-08T08:28:48.756Z",
    "domain": "RAAS-v1",
    "templateVersion": "v3"
  },
};
