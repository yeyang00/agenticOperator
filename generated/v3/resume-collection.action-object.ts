// AUTO-GENERATED by scripts/gen-action-snapshot.ts at 2026-05-08T08:28:48.337Z
// source: action=resumeCollection id=8 domain=RAAS-v1 template=v3
// DO NOT EDIT — regenerate via: npm run gen:ontology -- --action-name resumeCollection

import type { ActionObject } from "./action-object.types";

export const resumeCollectionActionObject: ActionObject = {
  id: "8",
  name: "resumeCollection",
  description: "简历收集 - 招聘专员从发布渠道收集和下载候选人简历",
  submissionCriteria: "",
  category: "人工操作",
  actor: [
    "Human"
  ],
  trigger: [
    "CHANNEL_PUBLISHED"
  ],
  targetObjects: [
    "Resume",
    "Job_Requisition"
  ],
  inputs: [
    {
      "description": "已发布的岗位发布信息编号，用于定位目标渠道。",
      "name": "job_posting_id",
      "required": true,
      "sourceObject": "Job_Posting.job_posting_id",
      "type": "String"
    }
  ],
  outputs: [
    {
      "description": "从各渠道下载的简历文件路径列表。",
      "name": "resume_file_paths",
      "type": "List<String>"
    },
    {
      "description": "简历关联的招聘岗位编号。",
      "name": "job_requisition_id",
      "type": "String"
    }
  ],
  actionSteps: [
    {
      "description": "招聘专员从各发布渠道（如BOSS直聘、智联招聘等）收集和下载候选人简历，整理后准备导入系统。",
      "inputs": [
        {
          "description": "岗位发布信息编号。",
          "name": "job_posting_id",
          "sourceObject": "Job_Posting.job_posting_id",
          "type": "String"
        }
      ],
      "name": "收集下载简历",
      "objectType": "action",
      "order": 1,
      "outputs": [
        {
          "description": "下载的简历文件路径列表。",
          "name": "resume_file_paths",
          "type": "List<String>"
        }
      ],
      "rules": []
    }
  ],
  sideEffects: {
    "dataChanges": [],
    "notifications": []
  },
  triggeredEvents: [
    "RESUME_DOWNLOADED"
  ],

  prompt: `## Action specification: resumeCollection

The block below is the formal specification of the action \`resumeCollection\`. It is part of a larger user message; the runtime supplies inputs and additional context separately. Treat this block as a binding contract: do not deviate from the declared steps, do not write outside the declared targets, and do not return outputs outside the declared schema. If preconditions are unmet or required inputs are missing, follow Error policy.

- Category: 人工操作
- Actor:    Human
- Trigger:  CHANNEL_PUBLISHED

## Purpose
简历收集 - 招聘专员从发布渠道收集和下载候选人简历

## Inputs
The runtime provides these as a JSON object on the inbound payload; field names match exactly. Validate before proceeding; if a required field is missing, follow Error policy.

- job_posting_id (String, required) ← Job_Posting.job_posting_id: 已发布的岗位发布信息编号，用于定位目标渠道。

## Steps
1. 收集下载简历 [action]
   description:  招聘专员从各发布渠道（如BOSS直聘、智联招聘等）收集和下载候选人简历，整理后准备导入系统。
   inputs:
   - job_posting_id (String) ← Job_Posting.job_posting_id — 岗位发布信息编号。
   outputs:
   - resume_file_paths (List<String>) — 下载的简历文件路径列表。

## Output
Return a JSON object matching this schema. The fields below are required; do not include extra keys.

- resume_file_paths (List<String>): 从各渠道下载的简历文件路径列表。
- job_requisition_id (String): 简历关联的招聘岗位编号。

\`\`\`json
{
  "resume_file_paths": ["<String>", "..."],
  "job_requisition_id": "<String>"
}
\`\`\`

## Side-effect boundary

### reads
Resume, Job_Requisition

### emits
- RESUME_DOWNLOADED

Anything outside the above is out of scope; refuse and surface via Error policy.

## Error policy
- Missing or invalid required input → return {"status":"failed","reason":"<which field>","retry":false}; do not partially execute.
- Upstream resource unreachable or auth failure → return {"status":"failed","reason":"<...>","retry":true}.

## Completion criteria
The action is complete when ALL of:

1. Each step has run to its declared termination, or its failure has been surfaced per Error policy.
2. The Output JSON contains all required keys (resume_file_paths, job_requisition_id) and conforms to the schema in Output.
3. Any required outbound event has been emitted (success events on success; alert events on failure).

## Before returning
Verify all of the following before responding:

- Output JSON conforms to the Output schema (no extra or missing keys).
- No write occurred outside Side-effect boundary.
- Every required notification or outbound event has been emitted.`,

  sections: {
    "actionSpec": "## Action specification: resumeCollection\n\nThe block below is the formal specification of the action `resumeCollection`. It is part of a larger user message; the runtime supplies inputs and additional context separately. Treat this block as a binding contract: do not deviate from the declared steps, do not write outside the declared targets, and do not return outputs outside the declared schema. If preconditions are unmet or required inputs are missing, follow Error policy.\n\n- Category: 人工操作\n- Actor:    Human\n- Trigger:  CHANNEL_PUBLISHED",
    "beforeReturning": "## Before returning\nVerify all of the following before responding:\n\n- Output JSON conforms to the Output schema (no extra or missing keys).\n- No write occurred outside Side-effect boundary.\n- Every required notification or outbound event has been emitted.",
    "completionCriteria": "## Completion criteria\nThe action is complete when ALL of:\n\n1. Each step has run to its declared termination, or its failure has been surfaced per Error policy.\n2. The Output JSON contains all required keys (resume_file_paths, job_requisition_id) and conforms to the schema in Output.\n3. Any required outbound event has been emitted (success events on success; alert events on failure).",
    "errorPolicy": "## Error policy\n- Missing or invalid required input → return {\"status\":\"failed\",\"reason\":\"<which field>\",\"retry\":false}; do not partially execute.\n- Upstream resource unreachable or auth failure → return {\"status\":\"failed\",\"reason\":\"<...>\",\"retry\":true}.",
    "inputsSpec": "## Inputs\nThe runtime provides these as a JSON object on the inbound payload; field names match exactly. Validate before proceeding; if a required field is missing, follow Error policy.\n\n- job_posting_id (String, required) ← Job_Posting.job_posting_id: 已发布的岗位发布信息编号，用于定位目标渠道。",
    "output": "## Output\nReturn a JSON object matching this schema. The fields below are required; do not include extra keys.\n\n- resume_file_paths (List<String>): 从各渠道下载的简历文件路径列表。\n- job_requisition_id (String): 简历关联的招聘岗位编号。\n\n```json\n{\n  \"resume_file_paths\": [\"<String>\", \"...\"],\n  \"job_requisition_id\": \"<String>\"\n}\n```",
    "preconditions": null,
    "purpose": "## Purpose\n简历收集 - 招聘专员从发布渠道收集和下载候选人简历",
    "ruleIndex": null,
    "sideEffectBoundary": "## Side-effect boundary\n\n### reads\nResume, Job_Requisition\n\n### emits\n- RESUME_DOWNLOADED\n\nAnything outside the above is out of scope; refuse and surface via Error policy.",
    "steps": "## Steps\n1. 收集下载简历 [action]\n   description:  招聘专员从各发布渠道（如BOSS直聘、智联招聘等）收集和下载候选人简历，整理后准备导入系统。\n   inputs:\n   - job_posting_id (String) ← Job_Posting.job_posting_id — 岗位发布信息编号。\n   outputs:\n   - resume_file_paths (List<String>) — 下载的简历文件路径列表。"
  },

  meta: {
    "actionId": "8",
    "actionName": "resumeCollection",
    "compiledAt": "2026-05-08T08:28:48.337Z",
    "domain": "RAAS-v1",
    "templateVersion": "v3"
  },
};
