// AUTO-GENERATED by scripts/gen-action-snapshot.ts at 2026-05-08T08:28:48.406Z
// source: action=resumeFix id=9-2 domain=RAAS-v1 template=v3
// DO NOT EDIT — regenerate via: npm run gen:ontology -- --action-name resumeFix

import type { ActionObject } from "./action-object.types";

export const resumeFixActionObject: ActionObject = {
  id: "9-2",
  name: "resumeFix",
  description: "简历解析修复 - 招聘专员处理简历解析错误，手动修正或重新上传简历",
  submissionCriteria: "",
  category: "人工操作",
  actor: [
    "Human"
  ],
  trigger: [
    "RESUME_PARSE_ERROR"
  ],
  targetObjects: [
    "Resume",
    "Candidate"
  ],
  inputs: [
    {
      "description": "解析失败的简历唯一编号。",
      "name": "resume_id",
      "required": true,
      "sourceObject": "Resume.resume_id",
      "type": "String"
    }
  ],
  outputs: [
    {
      "description": "修复结果状态。值为\"已修复\"(可重新解析)或\"候选人重新提交\"。",
      "name": "fix_result",
      "type": "String"
    }
  ],
  actionSteps: [
    {
      "description": "招聘专员处理简历解析错误。若为格式问题则手动修正解析结果；若为文件损坏则联系候选人重新提交简历后重新上传至系统。",
      "inputs": [
        {
          "description": "简历编号。",
          "name": "resume_id",
          "sourceObject": "Resume.resume_id",
          "type": "String"
        }
      ],
      "name": "修复简历解析",
      "objectType": "action",
      "order": 1,
      "outputs": [
        {
          "description": "修复结果。",
          "name": "fix_result",
          "type": "String"
        }
      ],
      "rules": []
    }
  ],
  sideEffects: {
    "dataChanges": [
      {
        "action": "MODIFY",
        "description": "重新上传简历后更新file_path和updated_time。",
        "objectType": "Resume",
        "propertyImpacted": [
          "file_path",
          "updated_time"
        ]
      }
    ],
    "notifications": []
  },
  triggeredEvents: [
    "RESUME_PROCESSED"
  ],

  prompt: `## Action specification: resumeFix

The block below is the formal specification of the action \`resumeFix\`. It is part of a larger user message; the runtime supplies inputs and additional context separately. Treat this block as a binding contract: do not deviate from the declared steps, do not write outside the declared targets, and do not return outputs outside the declared schema. If preconditions are unmet or required inputs are missing, follow Error policy.

- Category: 人工操作
- Actor:    Human
- Trigger:  RESUME_PARSE_ERROR

## Purpose
简历解析修复 - 招聘专员处理简历解析错误，手动修正或重新上传简历

## Inputs
The runtime provides these as a JSON object on the inbound payload; field names match exactly. Validate before proceeding; if a required field is missing, follow Error policy.

- resume_id (String, required) ← Resume.resume_id: 解析失败的简历唯一编号。

## Steps
1. 修复简历解析 [action]
   description:  招聘专员处理简历解析错误。若为格式问题则手动修正解析结果；若为文件损坏则联系候选人重新提交简历后重新上传至系统。
   inputs:
   - resume_id (String) ← Resume.resume_id — 简历编号。
   outputs:
   - fix_result (String) — 修复结果。

## Output
Return a JSON object matching this schema. The fields below are required; do not include extra keys.

- fix_result (String): 修复结果状态。值为"已修复"(可重新解析)或"候选人重新提交"。

\`\`\`json
{
  "fix_result": "<String>"
}
\`\`\`

## Side-effect boundary

### writes
- Resume / MODIFY
   fields: file_path, updated_time
   note:   重新上传简历后更新file_path和updated_time。

### reads
Candidate

### emits
- RESUME_PROCESSED

Anything outside the above is out of scope; refuse and surface via Error policy.

## Error policy
- Missing or invalid required input → return {"status":"failed","reason":"<which field>","retry":false}; do not partially execute.
- Upstream resource unreachable or auth failure → return {"status":"failed","reason":"<...>","retry":true}.

## Completion criteria
The action is complete when ALL of:

1. Each step has run to its declared termination, or its failure has been surfaced per Error policy.
2. The Output JSON contains all required keys (fix_result) and conforms to the schema in Output.
3. Any required outbound event has been emitted (success events on success; alert events on failure).

## Before returning
Verify all of the following before responding:

- Output JSON conforms to the Output schema (no extra or missing keys).
- No write occurred outside Side-effect boundary.
- Every required notification or outbound event has been emitted.`,

  sections: {
    "actionSpec": "## Action specification: resumeFix\n\nThe block below is the formal specification of the action `resumeFix`. It is part of a larger user message; the runtime supplies inputs and additional context separately. Treat this block as a binding contract: do not deviate from the declared steps, do not write outside the declared targets, and do not return outputs outside the declared schema. If preconditions are unmet or required inputs are missing, follow Error policy.\n\n- Category: 人工操作\n- Actor:    Human\n- Trigger:  RESUME_PARSE_ERROR",
    "beforeReturning": "## Before returning\nVerify all of the following before responding:\n\n- Output JSON conforms to the Output schema (no extra or missing keys).\n- No write occurred outside Side-effect boundary.\n- Every required notification or outbound event has been emitted.",
    "completionCriteria": "## Completion criteria\nThe action is complete when ALL of:\n\n1. Each step has run to its declared termination, or its failure has been surfaced per Error policy.\n2. The Output JSON contains all required keys (fix_result) and conforms to the schema in Output.\n3. Any required outbound event has been emitted (success events on success; alert events on failure).",
    "errorPolicy": "## Error policy\n- Missing or invalid required input → return {\"status\":\"failed\",\"reason\":\"<which field>\",\"retry\":false}; do not partially execute.\n- Upstream resource unreachable or auth failure → return {\"status\":\"failed\",\"reason\":\"<...>\",\"retry\":true}.",
    "inputsSpec": "## Inputs\nThe runtime provides these as a JSON object on the inbound payload; field names match exactly. Validate before proceeding; if a required field is missing, follow Error policy.\n\n- resume_id (String, required) ← Resume.resume_id: 解析失败的简历唯一编号。",
    "output": "## Output\nReturn a JSON object matching this schema. The fields below are required; do not include extra keys.\n\n- fix_result (String): 修复结果状态。值为\"已修复\"(可重新解析)或\"候选人重新提交\"。\n\n```json\n{\n  \"fix_result\": \"<String>\"\n}\n```",
    "preconditions": null,
    "purpose": "## Purpose\n简历解析修复 - 招聘专员处理简历解析错误，手动修正或重新上传简历",
    "ruleIndex": null,
    "sideEffectBoundary": "## Side-effect boundary\n\n### writes\n- Resume / MODIFY\n   fields: file_path, updated_time\n   note:   重新上传简历后更新file_path和updated_time。\n\n### reads\nCandidate\n\n### emits\n- RESUME_PROCESSED\n\nAnything outside the above is out of scope; refuse and surface via Error policy.",
    "steps": "## Steps\n1. 修复简历解析 [action]\n   description:  招聘专员处理简历解析错误。若为格式问题则手动修正解析结果；若为文件损坏则联系候选人重新提交简历后重新上传至系统。\n   inputs:\n   - resume_id (String) ← Resume.resume_id — 简历编号。\n   outputs:\n   - fix_result (String) — 修复结果。"
  },

  meta: {
    "actionId": "9-2",
    "actionName": "resumeFix",
    "compiledAt": "2026-05-08T08:28:48.406Z",
    "domain": "RAAS-v1",
    "templateVersion": "v3"
  },
};
